package interfaces;

public class interfaceDefinition {
	
	// interfaces can have only Abstract methods: writing only the methods decleation-
	// but not method 

	// Concrete class means there should not be any abstract method on it.
	
	
	// abstract class and abstract methods
	
	/* abstract method is only declaration but no definition
	 * 
	 * abstract Class : A class that contains at least one abstract method.
	 * abstract Class can have abstract methods and normal methods.
	 * Object cannot be created in Abstract class.
	 * 
	 * Implementation of abstract class will be in Derived class
	 */
	
	// Concrete class: contains complete definition for all the methods
	// Object can be created in concrete class
}
